<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sky.mapper.EmployeeMapper">
<!--注解sql语句只适合查询简单的select 和insert如果涉及到sql语句比较复杂，比如动态参数和动态查询-->
    <select id="pageQuery" resultType="com.sky.entity.Employee">
        select  * from employee
        <where>
            <if test="name!=null and name != '' ">
                and name like concat('%',#{name,jdbcType=NVARCHAR},'%');
            </if>

        </where>
-- 没招了，加上排序就冲突报错，数据库没用对，只能抽时间换成mysql了
--     order by create_time desc;
    </select>
    <update id="update" parameterType="Employee" >
        update employee
        <set>
            <if test="name !=null">
                name=#{name,jdbcType=NVARCHAR},
            </if>
            <if test="username !=null">
                username=#{username,jdbcType=NVARCHAR},
            </if>
            <if test="password !=null">
                password=#{password,jdbcType=NVARCHAR},
            </if>
            <if test="phone !=null">
               phone=#{phone,jdbcType=NVARCHAR},
            </if>
            <if test="sex !=null">
                sex=#{sex,jdbcType=NVARCHAR},
            </if>
            <if test="idNumber !=null">
                id_number=#{idNumber,jdbcType=NVARCHAR},
            </if>
            <if test="updateTime !=null">
                update_time=#{updateTime,jdbcType=TIMESTAMP},
            </if>
            <if test="status !=null">
                status=#{status,jdbcType=INTEGER}
            </if>
        </set>
      where id=#{id}
    </update>

</mapper>
